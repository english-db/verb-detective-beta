# Part Template

Use this folder as the model for new parts.

Recommended files for each future part:
- `config.js`: media paths and part-level metadata
- `data.js`: verbs, hotspots, QCM and room mapping
- `index.js`: exports the part definition consumed by the app

